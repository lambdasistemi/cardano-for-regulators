name: Build and deploy documentation

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: nixos
    steps:
      - name: Clean workspace
        run: chmod -R u+w . 2>/dev/null || true

      - uses: actions/checkout@v6
      - uses: cachix/cachix-action@v15
        with:
          name: paolino
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}

      - name: Build site
        run: |
          # 0. Clean stale build artifacts (nix store files are read-only)
          chmod -R u+w site 2>/dev/null || true
          rm -rf site

          # 1. Generate markdown pages from ontology
          nix develop .#default --quiet -c python3 ontology/render.py

          # 2. Build mkdocs
          nix develop github:paolino/dev-assets?dir=mkdocs --quiet -c mkdocs build --strict

          # 3. Copy ontology viewer into the built site
          nix build .#ontology-viewer
          mkdir -p site/viewer
          cp -rL result/index.html result/index.js result/data site/viewer/
          chmod -R u+w site/viewer

      - name: Deploy site
        run: nix develop github:paolino/dev-assets?dir=mkdocs --quiet -c ghp-import site --push --force --no-jekyll
